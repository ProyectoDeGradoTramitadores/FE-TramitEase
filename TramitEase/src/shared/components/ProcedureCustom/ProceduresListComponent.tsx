import React from 'react';import { Box } from '@mui/material';import ProcedureComponent from './ProcedureComponent.tsx';import { useNavigate, useParams } from 'react-router-dom';import { ProcedureListComponentProps } from '../../types/ProcedureComponentProps.ts';const ProceduresListComponent: React.FC<ProcedureListComponentProps> = ({ procedures }) => {    const { id } = useParams<{ id: string }>();    const navigate = useNavigate();    const handleTramitClick = (idProcedure: number) => {        navigate(`/TramitEase/Tramitador/${id}/Custom/TramitsCustom/ProcedureViewPage/${idProcedure}`);    };    return (        <Box            sx={{                padding: '20px 0 10px 0',                display: 'grid',                gridTemplateColumns: 'repeat(3, 1fr)',                gap: '20px',                rowGap: '40px',            }}        >            {procedures.map((procedure, index) => (                <Box                    key={index}                    sx={{                        display: 'flex',                        justifyContent: 'center',                        alignItems: 'center',                    }}                >                    <ProcedureComponent                        name={procedure.name || ""}                        daysDuring={procedure.dayDuring?.toString() || ''}                        onClick={() => handleTramitClick(procedure.idProcedure)}                    />                </Box>            ))}        </Box>    );};export default ProceduresListComponent;